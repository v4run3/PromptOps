apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: promptops
  namespace: promptops
spec:
  rules:
    - host: promptops.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: promptops
                port:
                  number: 80
